.root {
  height: 44px;
  display: flex;
  background: linear-gradient(top, #699467, lighten(#699467, 20%), #699467);
  color: #fff;
}

.left,
.middle,
.right {
  display: flex;
  flex: 1;
  align-items: center;
  justify-content: center;
}

.middle {
  flex: 3;
}
